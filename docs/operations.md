# `operations.yaml` reference

`~/.dbctl/operations.yaml` (or `~/.dbctl/profiles/<name>/operations.yaml`
when using `--profile <name>`) declares every SQL block `dbctl` can run. The
whole file is one map:

```yaml
operations:
  <name>:
    ...
```

Operations are **global** — every connection sees them. To restrict which
ops a connection can run, use the connection's
[`safety.allowed_operations`](connections.md#safety-block) whitelist.

There are two scopes:

- **`single`** — runs against one connection as `dbctl <conn> <op> ...`.
- **`multi`** — runs against the role-listed connections as
  `dbctl <verb> <op> <conn-a> <conn-b> ...`, where `<verb>` is the operation's
  `mode` (`diff` / `compare` / `sync`).

## Common fields

| field           | type                  | required | notes |
|-----------------|-----------------------|----------|-------|
| `description`   | string               | no       | shown in `dbctl operations list` and the autogenerated `--help`. |
| `scope`         | `single` \| `multi`   | no (default `single`) | whether the op runs against one connection or a role-list. |
| `namespace`     | string               | no       | reserved for `dbctl <conn> <ns> <op>` grouping (v2). |
| `mode`          | see [Mode](#mode)     | no (default `execute`) | how to dispatch the SQL. |
| `confirm`       | bool                 | no (default `true`)    | combined with `conn.safety.confirm` to decide dry-run vs commit. |
| `output`        | `table` \| `json` \| `csv` \| `yaml` (default `table`) | no | output format for `fetch` / `fetch_one` modes. |
| `tags`          | list of strings      | no       | reserved for `dbctl <conn> --tag ... <op>` filtering (v2). |

## `parameters`

Each parameter becomes a Click flag (or positional arg), is type-coerced
before the SQL is rendered, and is redacted in the audit log if its type is
`secret`.

```yaml
parameters:
  - name: user_id
    type: integer
    required: true
    position: 1                    # 1-based; null = keyword-only (--flag)
    description: "ID of the user to update"
    # default: ...                  # mutually exclusive with required
    # choices: [asc, desc]          # restricts CLI tab-completion values
    # completer: users              # reserved: server-side tab completion
```

| field         | type                                       | notes |
|---------------|--------------------------------------------|-------|
| `name`        | string                                     | the SQL `$name` placeholder. |
| `type`        | `string` / `integer` / `float` / `bool` / `date` / `path` / `list` / `secret` (default `string`) | coerces the value before binding. `list` splits a comma-separated string. `secret` redacts in audit logs and hides input in interactive prompts. |
| `required`    | bool                                       | if missing and `stdin` is a TTY, the CLI prompts interactively; in CI it errors. Mutually exclusive with `default`. |
| `default`     | any (matching the type)                     | used when not provided. Mutually exclusive with `required`. |
| `position`    | integer (1-based)                          | if set, becomes a positional `click.Argument`; otherwise a `--name` flag. Multiple positionals are ordered by `position`. |
| `description` | string                                    | shown in `--help`. |
| `choices`     | list of strings                            | reserved for `click.Choice` enforcement (v2). |
| `completer`   | string                                    | reserved for server-side shell completion (v2). |

## SQL placeholder syntax

Operations use `$name` placeholders — these are rewritten to SQLAlchemy
bind-params (`:name`) and **always parameterised**, never string-interpolated.
A parameter value can never become SQL, so SQL injection through a parameter
value is not possible.

```yaml
sql: |
  INSERT INTO users (name, quota_daily, quota_yearly, type)
  VALUES ($name, $quota, $quota * 365, $type)
  ON CONFLICT (name) DO UPDATE SET quota_daily = EXCLUDED.quota_daily
```

- `$1`, `$2`, … are **not** rewritten (they're positional placeholders in
  some dialects) — the regex only matches `$<identifier>`.
- PostgreSQL dollar-quoted strings (`$$ ... $$`) are not specially handled:
  avoid mixing dollar-quoting with `$name` parameters in the same operation.
  Use single-quoted string literals for any in-SQL text.
- The same placeholder can appear twice in one statement
  (`VALUES ($quota, $quota * 365)`); the driver reuses the value.

## <a name="mode"></a> `mode` (single-scope)

| mode        | behaviour |
|-------------|-----------|
| `execute`   | Run the parameterised `sql` inside `engine.begin()`; report rows affected. Subject to `safety.confirm` dry-run. |
| `fetch`     | Run the `sql`, render the result rows using `output` (`table` / `json` / `csv` / `yaml`). |
| `fetch_one` | Like `fetch` but returns the first row only (or empty result if no rows). |
| `script`    | Run the `sql` as a single statement (multi-statement scripts are v2; v1 runs the first statement). Subject to `safety.confirm`. |
| `upsert`    | Reserved for v2 — autoload the target table and build a dialect-aware `INSERT ... ON CONFLICT` / `ON DUPLICATE KEY` from a YAML fixture file. The `sql` field is optional for `upsert` (it builds the SQL from the file at runtime). |

## `mode` (multi-scope)

A multi-scope operation declares `roles` and one `queries.<role>` entry per
role. The CLI builds a top-level group **per distinct mode** (`diff`,
`compare`, `sync`), with one subcommand per op. Connections are passed
positionally in the declared role order.

### `diff`

```yaml
operations:
  user-count:
    description: "Compare user counts between two databases"
    scope: multi
    mode: diff
    roles: [src, trg]
    queries:
      src: "SELECT 'users' AS t, COUNT(*) AS n FROM users"
      trg: "SELECT 'users' AS t, COUNT(*) AS n FROM users"
    diff:
      key: [t]              # column(s) to join the two result sets on
      show: [n]             # optional: which non-key columns to render
```

- `roles` becomes the list of leading positional args of the
  `dbctl diff <op>` subcommand.
- `queries.<role>` runs against the connection passed at that role position.
- `diff.key` joins the two result sets; rows missing from either side are
  shown with a `∅` marker and counted in a "rows only in …" footnote.
- `diff.show` filters which non-key columns to render (defaults to all
  non-key columns from the first result set).

Invocation:

```bash
dbctl diff user-count pg my
dbctl diff user-count pg my --show-sql   # print per-role SQL before running
```

Output (rich table):

```text
┏━━━━━━━━┳──────────┳──────────┳━━━━━┓
┃ t      ┃ src.n   ┃ trg.n   ┃  Δ  ┃
┡━━━━━━━━╇──────────╇──────────╇━━━━━┩
│ users  │       3  │       3  │  0  │
└────────┴──────────┴──────────┴─────┘
```

### `compare` / `sync`

Reserved in v1 — the framework will follow the same shape as `diff`:

```yaml
operations:
  sync-feature-flags:
    scope: multi
    mode: sync
    roles: [src, trg]
    parameters:
      - { name: table, type: string, required: true, position: 1 }
    queries:
      src: "SELECT key, enabled FROM feature_flags"
      trg: "INSERT INTO $table ...; -- derived from src"
```

## Operations are dialect-specific

Operations are global, but the SQL they contain is dialect-specific
(`ON CONFLICT` is PostgreSQL; `ON DUPLICATE KEY UPDATE` is MySQL; `MERGE` is
SQL Server). For v1 the workaround is to either:

1. write **portable** SQL (e.g. `DELETE` + `INSERT` rather than upsert syntax)
   for ops you want to run on multiple dialects, or
2. scope the op by connection via `safety.allowed_operations` so it can only
   be invoked against the dialect it targets.

Per-connection operation overrides (one operations.yaml per profile) are on
the roadmap.

## Full example

```yaml
operations:
  add-user:
    description: "Create or update an application user (Daily quota by default)"
    scope: single
    mode: execute
    confirm: true
    parameters:
      - { name: name,  type: string,  required: true,  position: 1, description: "Unique user name" }
      - { name: quota, type: integer, required: true,  position: 2, description: "Daily quota" }
      - { name: type,  type: string,  default: "Daily", position: 3, description: "Account type" }
    sql: |
      INSERT INTO users (name, quota_daily, quota_yearly, type)
        VALUES ($name, $quota, $quota * 365, $type)
      ON CONFLICT (name) DO UPDATE
        SET quota_daily  = EXCLUDED.quota_daily,
            quota_yearly = EXCLUDED.quota_yearly,
            type         = EXCLUDED.type,
            updated_at   = NOW()

  list-users:
    description: "List users (top N)"
    scope: single
    mode: fetch
    output: table
    parameters:
      - { name: limit, type: integer, default: 10, position: 1 }
    sql: |
      SELECT name, quota_daily, quota_yearly, type, is_active
      FROM users
      ORDER BY quota_daily DESC
      LIMIT $limit

  find-user:
    description: "Find a user by name prefix"
    scope: single
    mode: fetch
    parameters:
      - { name: prefix, type: string, required: true, position: 1 }
    sql: |
      SELECT name, quota_daily, is_active FROM users WHERE name ILIKE $prefix || '%'

  report-logs:
    description: "Summarise logs by level between two dates"
    scope: single
    mode: fetch
    output: table
    parameters:
      - { name: since, type: string, required: true, position: 1, description: "ISO date" }
      - { name: until, type: string, required: true, position: 2, description: "ISO date" }
    sql: |
      SELECT level, COUNT(*) AS events, MIN(created_at) AS first_seen, MAX(created_at) AS last_seen
      FROM logs
      WHERE created_at >= $since::timestamp AND created_at < $until::timestamp
      GROUP BY level
      ORDER BY events DESC

  user-count:
    description: "Compare user counts between two databases"
    scope: multi
    mode: diff
    roles: [src, trg]
    queries:
      src: "SELECT 'users' AS t, COUNT(*) AS n FROM users"
      trg: "SELECT 'users' AS t, COUNT(*) AS n FROM users"
    diff:
      key: [t]
      show: [n]

  compare-quotas:
    description: "Side-by-side quota summary across two databases"
    scope: multi
    mode: diff
    roles: [src, trg]
    parameters:
      - { name: period, type: string, default: "Daily", position: 1 }
    queries:
      src: |
        SELECT period AS t, COUNT(*) AS n_rows, SUM(limit_value) AS total_limit
        FROM quotas JOIN users ON quotas.user_id = users.id
        WHERE period = $period GROUP BY period
      trg: |
        SELECT period AS t, COUNT(*) AS n_rows, SUM(limit_value) AS total_limit
        FROM quotas JOIN users ON quotas.user_id = users.id
        WHERE period = $period GROUP BY period
    diff:
      key: [t]
      show: [n_rows, total_limit]
```

## Safety check matrix

| connection `safety.confirm` | op `confirm` | `--apply` | result |
|----------------------------|-------------|-----------|--------|
| `true`                     | `true`      | absent    | **dry-run** — print resolved SQL, write `status: dry-run` audit, exit 0 |
| `true`                     | `true`      | `--apply` | prompt `[y/N]` (skip with `--yes`); on yes, commit |
| `false`                    | `true`      | absent    | commit (the op-level `confirm` alone is not enough) |
| `false`                    | `false`     | n/a       | commit |
| (any)                      | (any)       | (any)     | if `safety.read_only: true`, DML is blocked entirely (exit 6) |
| (any)                      | (any)       | (any)     | if `safety.allowed_operations: [a, b]` and op not in it, exit 6 |

For `fetch` / `fetch_one` modes the confirm gate is irrelevant — they always
run read-only.